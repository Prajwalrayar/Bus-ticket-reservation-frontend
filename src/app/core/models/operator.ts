export interface OperatorDTO {
  operatorId?: string;
  companyName: string;
  contactEmail: string;
  contactPhone: string;
  logoUrl?: string;
  websiteUrl?: string;
  isApproved?: boolean;
  isActive?: boolean;
}
